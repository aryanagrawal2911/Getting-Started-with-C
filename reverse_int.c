# include <stdio.h>
# include <math.h>

static int reverse(int x){
	int sum, rev, c = 0;
	
	while(x != 0){
		
	}
}

int main(){

	int num;
	printf("Enter a number : ");
	scanf("%d", &num);
	
	printf(reverse(num), "\n");
}
